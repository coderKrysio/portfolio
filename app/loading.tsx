export default function Loading() {
    return (
        <div 
        className = "fixed inset-0 text-white z-[10000] flex flex-1 items-center justify-center">
           <h1 className="text-6xl font-bold">{"LOADING....."}</h1>
        </div>
    );
}